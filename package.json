{
  "name": "running-heatmap",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted Strava running heatmap generator and embeddable widget",
  "scripts": {
    "authorize:strava": "node scripts/authorize-strava.js",
    "sync:strava": "node scripts/sync-strava.js"
  }
}
